#include "main.h"
#include <stdio.h>

/**
 * main - check the code.
 *
 * Return: Always 0.
 */
int _isupper(int c);

{

	int c = alphabet
		if (isupper(c))
		cout << c << "1 \n";
		{
			return (1);
		}
	else
	{
		return (0);
	}
}

